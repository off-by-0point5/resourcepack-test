{
  "entrypoint": "${group}.${name.toLowerCase()}.ExtensionMain",
  "name": "${name}",
  "version": "${version}",
  "dependencies": [

  ],
  "externalDependencies": {
    "repositories": [

    ],
    "artifacts": [

    ]
  }
}